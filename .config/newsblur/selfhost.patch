diff --git a/apps/reader/forms.py b/apps/reader/forms.py
index 5b9c8f9..61c097e 100644
--- a/apps/reader/forms.py
+++ b/apps/reader/forms.py
@@ -209,7 +209,9 @@ class SignupForm(forms.Form):
         if new_user.email:
             EmailNewUser.delay(user_id=new_user.pk)
 
-        if getattr(settings, "AUTO_PREMIUM_NEW_USERS", False):
+        if getattr(settings, "AUTO_PREMIUM_ARCHIVE_NEW_USERS", False):
+            new_user.profile.activate_archive()
+        elif getattr(settings, "AUTO_PREMIUM_NEW_USERS", False):
             new_user.profile.activate_premium()
         elif getattr(settings, "AUTO_ENABLE_NEW_USERS", False):
             new_user.profile.activate_free()
diff --git a/docker/haproxy/haproxy.docker-compose.cfg b/docker/haproxy/haproxy.docker-compose.cfg
index f3d98b7..bb3cdae 100644
--- a/docker/haproxy/haproxy.docker-compose.cfg
+++ b/docker/haproxy/haproxy.docker-compose.cfg
@@ -18,7 +18,7 @@ global
 # fails to start if any backend hostname can't be resolved at load time, which
 # takes the whole site offline for an unrelated container crash.
 resolvers docker
-    nameserver dns1 127.0.0.11:53
+    nameserver dns1 10.89.50.1:53
     resolve_retries 3
     timeout resolve 1s
     timeout retry 1s
@@ -46,14 +46,11 @@ defaults
     errorfile 504 /srv/newsblur/templates/502.http
 
 frontend public
-    bind :80
-    bind :443 ssl crt /srv/newsblur/config/certificates/localhost.pem #ciphers ECDHE-RSA-AES128-GCM-SHA256:ECDHE-RSA-AES256-GCM-SHA384:ECDHE-RSA-AES128-SHA256:ECDHE-RSA-AES128-SHA:ECDHE-RSA-AES256-SHA384:ECDHE-RSA-AES256-SHA:AES128-GCM-SHA256:AES256-GCM-SHA384:AES128-SHA256:AES128-SHA:AES256-SHA256:AES256-SHA no-sslv3
-    http-response add-header Strict-Transport-Security max-age=31536000;\ includeSubDomains
+    bind :8082
     http-request set-header X-Request-Start %[date()]%[date_us()]
     option http-server-close
 
-    # Redirect all HTTP traffic to HTTPS
-    redirect scheme https if !{ ssl_fc }
+    # HTTP is intentionally retained for direct LAN and tailnet access.
     
     # Block vulnerability scanners before they reach Gunicorn workers
     acl is_scanner_path path_end .php .cgi .asp .aspx .env
@@ -130,7 +127,7 @@ backend nginx
 backend gunicorn
     balance roundrobin
     # option httpchk GET /_haproxychk
-    server app_django nb.com:8000 check inter 3000ms resolvers docker init-addr last,libc,none
+    server app_django newsblur_web:8000 check inter 3000ms resolvers docker init-addr last,libc,none
 
 backend postgres
     server db_postgres newsblur_db_postgres:5432 check inter 2000ms resolvers docker init-addr last,libc,none
@@ -160,7 +157,7 @@ backend mcp
     server newsblur_mcp newsblur_mcp:8099 check inter 3000ms resolvers docker init-addr last,libc,none
 
 listen stats
-bind :1936 ssl crt /srv/newsblur/config/certificates/localhost.pem
+bind :1936
 stats enable
 stats hide-version
 stats realm Haproxy\ Statistics
