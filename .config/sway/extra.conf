# Personal additions after the baseline works.
# Keep Syncthing managed by its systemd user service (see the install guide).
# Steam and Discord launch from the main Sway config and use workspace rules.
